name: dsh_remote_control
description: Android controller for the DSH Codex Kit with Bluetooth onboarding and HTTPS relay control.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: 2.3.12
  permission_handler: 13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
